# Development / test-only dependencies.
# Runtime dependencies are in requirements.txt (this file includes them).
# Install with:  python -m pip install -r requirements-dev.txt
-r requirements.txt

pytest
httpx